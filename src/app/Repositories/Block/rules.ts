export const blockRules: { [key: string]: string } = {
  title: "required|string|max:255",
  data_type: "required|string",
  input_type: "required|string",
  type: "required|string",
  icon: "required|string",
};
