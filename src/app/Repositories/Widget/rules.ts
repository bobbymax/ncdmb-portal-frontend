export const widgetRules: { [key: string]: string } = {
  document_type_id: "required|integer",
  title: "required|string",
  component: "required|string",
  response: "required|string",
  type: "required|string",
  groups: "required|array",
};
