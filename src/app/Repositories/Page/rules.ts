export const pageRules: { [key: string]: string } = {
  name: "required|string",
  path: "required|string",
  icon: "required|string",
  description: "string",
  type: "required|string",
  roles: "required|array",
};
