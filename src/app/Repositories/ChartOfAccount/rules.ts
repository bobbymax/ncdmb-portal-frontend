export const chartOfAccountRules: { [key: string]: string } = {
  account_code: "required|string",
  name: "required|string",
  type: "required|string",
  level: "required|string",
  status: "required|string",
};
