import logo from "../../../assets/images/logo.png";

const LogoWidget = () => {
  return (
    <img
      style={{
        width: 95,
      }}
      alt="NCDMB Logo"
      src={logo}
    />
  );
};

export default LogoWidget;
