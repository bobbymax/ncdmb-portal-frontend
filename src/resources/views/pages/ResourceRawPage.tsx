import { BaseRepository } from "app/Repositories/BaseRepository";
import { PageProps } from "bootstrap";

const ResourceRawPage = ({
  Repository,
  view,
  Component,
}: PageProps<BaseRepository>) => {
  return <div>ResourceRawPage</div>;
};

export default ResourceRawPage;
